
{% extends "layout/layout.twig" %}

{% block main %}

    <!DOCTYPE html>
    <html>
    <head>
        <script src="https://cdn.tiny.cloud/1/no-api-key/tinymce/5/tinymce.min.js" referrerpolicy="origin"></script>
        <script>tinymce.init({ selector:'textarea' });</script>
    </head>
    </html>


        <h2 id="top">Panneau de contrôle :</h2>

        <header class="admin-absolut">
            <h3>Navigation</h3>


                <ul>
                    <li><a href="#chapters"><i class="fas fa-book"></i> Gestion des chapitres</a></li>
                    <li><a href="#comments"><i class="fas fa-comments"></i> Voir les commentaires signalés</a></li>
                    <li><a href="#users"><i class="fas fa-user"></i> Liste des utilisateurs enregistrés</a></li>
                </ul>

        </header>


        <article>
            <h3>Création d'un article</h3>

            <form method="post" action="{{ url('articles!create') }}" enctype="multipart/form-data">
                <ul>
                    <li>
                        <label for="title">Titre</label>
                        <input id="title" type="text" name="title" placeholder="Titre">
                    </li>

                    <li>
                        <label for="content">Contenu du chapitre</label>
                        <textarea name="content" id="content"></textarea>
                    </li>

                    <li>
                        <button type="submit">Créer</button>
                    </li>
                </ul>
            </form>
        </article>

        <article class="admin-section">
            <h3 id="chapters">Espace de gestion des chapitres créés</h3>

            <table>
                <caption>Recensement des chapitres</caption>
                <thead>
                <tr>
                    <th scope="col">Titre du chapitre</th>
                    <th scope="col">Editer / Supprimer</th>
                </tr>
                </thead>
                <tbody>
                {% for article in articles %}
                    <tr>
                        <td>{{ article.title }}</td>
                        <td>
                            <a href="{{ url('Articles!modify', {'id': article.id}, {'id': comment.article_id}) }}">
                                <i class="fas fa-pen-square fa-fw fa-lg"></i>
                            </a>
                            <a href="{{ url('Articles!delete', {'id': article.id}) }}">
                                <i class="fas fa-times-circle fa-fw fa-lg"></i>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <h3 id="comments">Espace de gestion des commentaires créés</h3>

            <table>
                <caption>Commentaires signalés<i class="fas fa-exclamation-triangle"></i></caption>
                <thead>
                <tr>
                    <th scope="col">Auteur</th>
                    <th scope="col">Contenu</th>
                    <th scope="col">Poster / Supprimer</th>
                </tr>
                </thead>
                <tbody>
                {% for comment in comments %}
                    {% if comment.moderated == 1 %}
                        <tr>
                            <td>{{ comment.author }} </td>
                            <td>{{ comment.content | nl2br }} </td>
                            <td><a href="{{ url('Comments!approved', {'id': comment.id}) }}">Poster</a></td>
                            <td><a class="btn-delete" href="#">Supprimer<i class="fas fa-times-circle fa-fw fa-lg"></i></a></td>
                        </tr>
                    {% endif %}
                {% endfor %}
                </tbody>
            </table>

            <h3 id="users">Espace de gestion des utilisateurs inscrits</h3>

            <table>
                <caption>Tableau des utilisateurs</caption>
                <thead>
                <tr>
                    <th scope="col">nom affiché</th>
                    <th scope="col">Email</th>
                    <th scope="col">Supprimer utilisateur</th>
                </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.name }}</td>
                        <td>{{ user.email }}</td>
                        <td>
                            <a href="{{ url('Users!delete', {'id': user.id}) }}">
                                <i class="fas fa-times-circle fa-fw fa-lg"></i>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </article>


{% endblock %}